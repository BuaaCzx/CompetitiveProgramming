#include <bits/stdc++.h>
using namespace std;
int main() {
    freopen("collect_all_facts.sh", "r", stdin);
    char c;
    while (cin >> c) {
        if (c == '\r') {
            cout << "!!!\n";
        }
    }
    return 0;
}
